extend schema @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key"])

type Session @key(fields: "id") {
  id: ID!
  averageRating: Float
  ratingsCount: Int
}

type Mutation {
  rateSession(sessionId: ID!, rating: Int!): Session
}


